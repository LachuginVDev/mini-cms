# Mini CMS (Laravel + Tailwind + Docker)

Учебный проект современной CMS, построенной на актуальном стеке:

- **Laravel 12** - PHP фреймворк
- **Tailwind CSS 3.4** - Utility-first CSS фреймворк
- **Vite 7** - Сборщик фронтенда
- **MySQL 8.0** (Docker) - База данных
- **PHP 8.2+** - Язык программирования
- **Laravel Breeze** - Аутентификация
- **Pest** - Тестирование

---

## 🚀 Возможности

### Публичная часть
- Просмотр списка опубликованных постов с пагинацией
- Просмотр отдельного поста по slug
- Фильтрация постов по статусу (только опубликованные)
- Отображение категорий постов
- Breadcrumbs навигация

### Административная панель
- CRUD операции для постов (создание, редактирование, удаление)
- Управление статусами постов (draft, published, archived)
- Привязка постов к категориям
- Middleware для защиты админ-панели

### Система аутентификации
- Регистрация пользователей
- Вход/выход из системы
- Восстановление пароля
- Профиль пользователя
- Роли пользователей (admin, editor, manager)

### Технические особенности
- MVC-архитектура Laravel
- Миграции базы данных
- Eloquent ORM с отношениями
- Factories & Seeders для генерации тестовых данных
- Form Request валидация
- Современный UI на Tailwind CSS
- Горячая перезагрузка через Vite
- База данных в Docker-контейнере

---

## 📁 Структура проекта

```
mini-cms/
├── app/
│   ├── Http/
│   │   ├── Controllers/
│   │   │   ├── Admin/PostController.php  # CRUD для постов
│   │   │   ├── HomeController.php        # Главная страница
│   │   │   ├── PostController.php        # Публичные посты
│   │   │   └── ProfileController.php     # Профиль пользователя
│   │   ├── Middleware/
│   │   │   └── AdminMiddleware.php       # Проверка прав администратора
│   │   └── Requests/
│   │       └── PostRequest.php           # Валидация постов
│   └── Models/
│       ├── Post.php                      # Модель поста
│       ├── Category.php                  # Модель категории
│       ├── Comment.php                   # Модель комментария
│       └── User.php                      # Модель пользователя
├── database/
│   ├── migrations/                       # Миграции БД
│   ├── factories/                       # Фабрики для генерации данных
│   └── seeders/                         # Сидеры для заполнения БД
├── resources/
│   ├── views/                           # Blade шаблоны
│   │   ├── posts/                       # Шаблоны постов
│   │   ├── admin/                       # Админ-панель (требуется реализация)
│   │   └── layouts/                     # Основные layout'ы
│   ├── css/app.css                      # Стили Tailwind
│   └── js/app.js                        # JavaScript
├── routes/
│   ├── web.php                          # Веб-маршруты
│   └── auth.php                         # Маршруты аутентификации
└── docker-compose.yml                   # Конфигурация Docker
```

---

## ⚙️ Установка

### Требования
- PHP 8.2 или выше
- Composer
- Node.js и npm
- Docker и Docker Compose (для MySQL)

### 1. Клонирование репозитория
```bash
git clone https://github.com/LachuginVDev/mini-cms.git
cd mini-cms
```

### 2. Установка зависимостей
```bash
# PHP зависимости
composer install

# JavaScript зависимости
npm install
```

### 3. Настройка окружения
```bash
# Копирование файла окружения
cp .env.example .env

# Генерация ключа приложения
php artisan key:generate
```

### 4. Настройка базы данных

Отредактируйте `.env` файл:
```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=mini_cms
DB_USERNAME=laravel
DB_PASSWORD=secret
```

### 5. Запуск Docker контейнера с MySQL
```bash
docker-compose up -d
```

### 6. Выполнение миграций и заполнение БД
```bash
# Миграции
php artisan migrate

# Заполнение тестовыми данными
php artisan db:seed
```

### 7. Сборка фронтенда
```bash
# Для разработки (с hot reload)
npm run dev

# Для продакшена
npm run build
```

### 8. Запуск сервера разработки
```bash
php artisan serve
```

Приложение будет доступно по адресу: `http://localhost:8000`

---

## 👤 Учетные данные по умолчанию

После выполнения `php artisan db:seed` создается администратор:

- **Email:** `test@example.com`
- **Пароль:** `secret`
- **Роль:** `admin`

---

## 🗄️ Структура базы данных

### Таблицы

**users**
- id, name, email, password, role, email_verified_at, remember_token, timestamps

**posts**
- id, title, slug (unique), content, meta_title, meta_description, image, status, category_id, timestamps
- Статусы: `draft`, `published`, `archived`

**categories**
- id, title, slug (unique), timestamps

**comments**
- id, post_id, user_id, content, is_approved, timestamps

### Отношения

- `Post` belongsTo `Category`
- `Post` hasMany `Comment`
- `Category` hasMany `Post`
- `User` hasMany `Comment`
- `Comment` belongsTo `Post` и `User`

---

## 🛣️ Маршруты

### Публичные
- `GET /` - Главная страница
- `GET /posts` - Список опубликованных постов
- `GET /posts/{slug}` - Просмотр поста

### Административные (требуют авторизации и роль admin)
- `GET /admin` - Админ-панель
- `GET /admin/posts` - Список всех постов
- `GET /admin/posts/create` - Создание поста
- `POST /admin/posts` - Сохранение поста
- `GET /admin/posts/{post}` - Просмотр поста
- `GET /admin/posts/{post}/edit` - Редактирование поста
- `PUT/PATCH /admin/posts/{post}` - Обновление поста
- `DELETE /admin/posts/{post}` - Удаление поста

### Аутентификация
- `GET /login` - Форма входа
- `POST /login` - Авторизация
- `GET /register` - Форма регистрации
- `POST /register` - Регистрация
- `POST /logout` - Выход
- `GET /profile` - Профиль пользователя

---

## 🔧 Разработка

### Запуск в режиме разработки
```bash
# В одном терминале - сервер
php artisan serve

# В другом терминале - Vite (hot reload)
npm run dev
```

### Тестирование
```bash
php artisan test
```

### Форматирование кода
```bash
./vendor/bin/pint
```

### Очистка кэша
```bash
php artisan cache:clear
php artisan config:clear
php artisan view:clear
php artisan route:clear
```

---

## 📝 Заметки по реализации

### Что реализовано хорошо
- ✅ Правильная структура MVC
- ✅ Использование Eloquent ORM и отношений
- ✅ Миграции с правильными foreign keys
- ✅ Factories для генерации тестовых данных
- ✅ Middleware для защиты админ-панели
- ✅ Form Request валидация
- ✅ Scope для фильтрации опубликованных постов
- ✅ Автоматическая генерация slug при создании поста

### Что требует доработки
- ⚠️ Views для админ-панели (контроллер готов, но нет шаблонов)
- ⚠️ CRUD для категорий
- ⚠️ Управление комментариями
- ⚠️ Загрузка изображений (сейчас только URL)
- ⚠️ Поиск по постам
- ⚠️ Фильтрация по категориям на публичной части

---

## 🐳 Docker

Проект использует Docker для MySQL базы данных:

```bash
# Запуск контейнера
docker-compose up -d

# Остановка контейнера
docker-compose down

# Просмотр логов
docker-compose logs -f mysql
```

---

## 📚 Технологии

- **Laravel 12** - PHP фреймворк
- **Laravel Breeze** - Аутентификация
- **Tailwind CSS 3.4** - CSS фреймворк
- **Vite 7** - Сборщик
- **Alpine.js** - Легковесный JS фреймворк
- **Pest** - PHP тестирование
- **MySQL 8.0** - База данных
- **Docker** - Контейнеризация

---

## 📄 Лицензия

MIT License

---

## 👨‍💻 Автор

Проект создан в учебных целях для изучения Laravel.
